#To calculate the Area of the Triangle, we need base and hight
base = float(input("Enter the base:"))
hight = float(input("Enter the hight:"))

#Now let's calculate area, Area = 1/2 * base * hight

Area = 0.5 * base * hight

print(f"Area of Triangle is:{Area}")