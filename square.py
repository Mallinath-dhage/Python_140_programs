# Area of a square
side = float(input("Enter the length of a side of the square: "))
area = side * side
print(f"The area of the square with side length", {side}, "is:", {area})

# Here we are taking the length of a side of the square as input from the user. 
# We then calculate the area of the square using the formula area = side * side.